{
	admin off
	auto_https off
}

:80 {
	encode zstd gzip

	handle /up {
		respond "ok" 200
	}

	handle /api/* {
		reverse_proxy server:4000
	}

	handle /collab {
		reverse_proxy server:4000
	}

	handle /health {
		reverse_proxy server:4000
	}

	handle {
		reverse_proxy web:3000
	}
}
